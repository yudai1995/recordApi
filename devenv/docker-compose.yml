services:
    mongodb:
        image: mongo:latest
        ports:
            - '27017:27017'
        environment:
            TZ: Asia/Tokyo
            MONGO_INITDB_ROOT_USERNAME: Admin
            MONGO_INITDB_ROOT_PASSWORD: password
            MONGO_INITDB_DATABASE: recorder
